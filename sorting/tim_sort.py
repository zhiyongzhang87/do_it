# find runs in orignial array
# a run is a segment already sorted
# compute minimum run A
# size = min run size
# divide A into subarrays of size min run
# for each subarray
#   run insertion sort
# while size < n
#   for each subarray
#       merge(sub1, sub2)
#       size = size * 2